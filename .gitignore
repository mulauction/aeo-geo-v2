
# dev snapshots
server/data/snapshots/*.json

# dev snapshot artifacts
server/data/snapshots/*.json

# usage events (dev log)
server/data/usage-events.jsonl
data/snapshots/

# --- telemetry raw logs (never commit) ---
server/data/telemetry/raw/*.jsonl
server/data/telemetry/raw/*.ndjson
server/data/telemetry/raw/*.log
server/data/telemetry/raw/sample-export.jsonl

# --- telemetry rollups (derived; never commit) ---
server/data/telemetry/summary/*.json

# --- general runtime logs ---
*.log
*.jsonl
*.ndjson
